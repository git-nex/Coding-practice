#include <bits/stdc++.h>
using namespace std;


int solve(vector<int> &arr,int x,int k)
{
  solve(arr,x+1,k)
}



int main()
{
  int t,k;
  cin>>t;
  for(int k=0;k<t;k++)
  {
    int n;
    cin>>n;
    vector<int> arr;
    for(int i=0;i<n;i++)
    {
      cin>>num;
      arr.push_back(num);
    }
  }
}
